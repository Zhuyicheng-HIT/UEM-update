# 验证记录（2026-09-20）

环境：Windows、Python 3.12.7、PyTorch 2.12.0 CPU、Lightning 2.4.0。

- **16 项测试通过**：覆盖开关关闭、空/全/部分历史 mask、逐调用覆盖、每一步插值状态、最终固定值、非法输入、调用者张量不变、带 padding 的加权损失和 x0 恢复。
- 合成数据上的 Lightning 训练与验证完成 1 个优化器更新，包含 EMA；损失有限、权重确实更新，训练后的 keep/release 采样正常。
- 训练、采样和评估入口导入成功；源码编译通过；未发现指向已删除模块的内部导入。
- [与原始 E7 的数值对照](upstream_e7_parity.json) 中全部记录的差值为 0，包括 80 帧 Euler10 输出及抽查的训练梯度。完整模型 state_dict 严格加载成功，优化器训练参数顺序一致。
- 最后补充显式采样设备传递后，重新运行完整模型训练/采样测试，1 项通过。

对照使用随机初始化的 E7 权重、合成输入和 CPU float32。尚未验证真实预训练 checkpoint 精度、真实数据解码/FK 一致性、CUDA/bfloat16、多 GPU 训练、端到端延迟或历史约束的实际收益。分支不含训练好的 E7 权重、数据集和 SMPL-X 模型资产；可选的可视化/TMR 路径未运行。
